# Evidence: output of spike/verify_telethon_contract.py
# Generated: 2026-09-15T14:59:00Z
# Command:   uv run --with telethon==1.45.0 python spike/verify_telethon_contract.py
# Exit code: 0


==============================================================================
Telethon 1.45.0 on Python 3.11.2
==============================================================================

==============================================================================
1. MTProxy transport classes
==============================================================================
[PASS] ConnectionTcpMTProxyRandomizedIntermediate is exported
[INFO] TcpMTProxy docstring warns 'EXPERIMENTAL': True
       The support for TcpMTProxy classes is **EXPERIMENTAL** and prone to
       be changed. You shouldn't be using this class yet.

==============================================================================
2. TelegramClient construction contract
==============================================================================
[PASS] accepts `connection=`
[PASS] accepts `proxy=`
[PASS] accepts `timeout=`
[PASS] accepts `auto_reconnect=`
[PASS] accepts `receive_updates=`

==============================================================================
3. connect() return contract   <-- spike/REPORT.md got this wrong
==============================================================================
[INFO] signature: (self: 'TelegramClient') -> None
[PASS] connect() is annotated `-> None`, not bool -- return_annotation=None
[PASS] connect() never returns a truthy value -- `if await client.connect():` is therefore always False
[PASS] connect() issues a real MTProto RPC (help.GetConfigRequest) -- success is proven by this round-trip, not by a boolean
[PASS] connect() does not call start()/sign_in()
[PASS] connect() spawns background loops that must be torn down

==============================================================================
4. Introspection and teardown
==============================================================================
[PASS] is_connected is a METHOD in Telethon (a property in Pyrogram) -- type=function; `if client.is_connected:` is always truthy
[PASS] MemorySession exists for ephemeral, on-disk-free sessions -- passing a *string* session name instead writes a .session SQLite file
[INFO] disconnect() shields teardown with asyncio.shield: True
[INFO] disconnect() returns a coroutine/Task when the loop is running,
       therefore it must always be awaited.

==============================================================================
5. Secret formats accepted by the MTProxy transport   <-- CRITICAL
==============================================================================
[INFO] TcpMTProxy.normalize_secret present: True
[INFO] introduced in Telethon 1.35.0 (1.34.0 does not have it)
        @staticmethod
        def normalize_secret(secret):
            if secret[:2] in ("ee", "dd"):  # Remove extra bytes
                secret = secret[2:]
    
            try:
                secret_bytes = bytes.fromhex(secret)
            except ValueError:
                secret = secret + '=' * (-len(secret) % 4)
                secret_bytes = base64.b64decode(secret.encode())
    
            return secret_bytes[:16]  # Remove the domain from the secret (until domain support is added)

MTProxyIO.init_header() validation, verbatim:
        def init_header(secret, dc_id, packet_codec):
            # Validate
            is_dd = (len(secret) == 17) and (secret[0] == 0xDD)
            is_rand_codec = issubclass(
                packet_codec, RandomizedIntermediatePacketCodec)
            if is_dd and not is_rand_codec:
                raise ValueError(
                    "Only RandomizedIntermediate can be used with dd-secrets")
            secret = secret[1:] if is_dd else secret

format                                    hex  norm  result
------------------------------------------------------------------------------------------------
legacy 16-byte (32 hex)                    32    16  ACCEPTED (header built, no I/O)
dd-secret 17-byte 0xDD (34 hex)            34    16  ACCEPTED (header built, no I/O)
fake-TLS 0xEE, no SNI (34 hex)             34    16  ACCEPTED (header built, no I/O)
fake-TLS 0xEE + SNI google.com             62    16  ACCEPTED (header built, no I/O)
spike 'valid' case secret                  54    16  ACCEPTED (header built, no I/O)
spike 'wrong_secret' case                  54    16  ACCEPTED (header built, no I/O)
spike 'dead_endpoint' case                 34    16  ACCEPTED (header built, no I/O)
spike 'non_mtproto' case                   34    16  ACCEPTED (header built, no I/O)

[PASS] legacy 16-byte secrets are accepted
[PASS] dd-secrets are accepted
[PASS] fake-TLS (0xEE) acceptance matches the installed version -- accepted=True, normalize_secret=True

[INFO] MTProxyIO implements TLS ClientHello / SNI emulation: False
[INFO] 1.35.0+ truncates the secret to 16 bytes and DROPS the SNI domain
       ('until domain support is added' -- upstream comment). So an 0xEE
       secret is structurally usable, but whether a real fake-TLS MTProxy
       accepts the resulting handshake is EMPIRICALLY UNVERIFIED here.
       Resolving that needs a live test (Task 005/016), not source reading.
[PASS] TcpMTProxy takes the secret from proxy[2] (3-tuple server/port/secret)

==============================================================================
6. Built-in latency floor in TcpMTProxy._connect
==============================================================================
[INFO] # Wait for EOF for 2 seconds (or if _wait_for_data's definition
[INFO] await asyncio.wait_for(self._reader._wait_for_data('proxy'), 2)
[INFO] if self._reader.at_eof():
[PASS] TcpMTProxy waits up to 2s after connecting (issue #1134 workaround) -- a hard floor on MTProto connect latency that timeouts must budget for
[PASS] TcpMTProxy raises ConnectionError if the proxy closes immediately -- this is the observable signal for a non-MTProto endpoint

==============================================================================
SUMMARY
==============================================================================
All expectations held for this Telethon version.
Task 005 must be implemented against THESE facts, not spike/REPORT.md.
